<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Buff Bets</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #cfb87c;
            color: #000;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 80%;
            margin: 30px auto;
            background: #000;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.3);
            color: #cfb87c;
        }

        h1, h2 {
            text-align: center;
            color: #cfb87c;
        }

        .top-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .account-balance, .won-bets {
            font-size: 20px;
            font-weight: bold;
            background-color: #333;
            padding: 10px 20px;
            border-radius: 8px;
            border: 2px solid #cfb87c;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background-color: #222;
            color: #fff;
        }

        th, td {
            padding: 12px;
            border: 1px solid #cfb87c;
            text-align: center;
        }

        th {
            background-color: #444;
        }

        tr:nth-child(even) {
            background-color: #333;
        }

        .win {
            color: #4caf50;
            font-weight: bold;
        }

        .loss {
            color: #f44336;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{username}}'s Profile</h1>

        <div class="top-info">
            <div class="account-balance">Account Balance: ${{balance}}</div>
            <div class="won-bets">Total Bets Won: {{totalWins}}</div>
        </div>

        <h2>Past Bets</h2>
        {{#if bets.length}}
        <table>
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Event</th>
                    <th>Wager</th>
                    <th>Result</th>
                    <th>Payout</th>
                </tr>
            </thead>
            <tbody>
                {{#each bets}}
                <tr>
                    <td>{{this.time}}</td>
                    <td>{{this.event}}</td>
                    <td>${{this.amount}}</td>
                    <td class="{{#if this.result}}win{{else}}loss{{/if}}">
                        {{#if this.result}}Win{{else}}Loss{{/if}}
                    </td>
                    <td>
                        {{#if this.result}}
                            ${{this.amount}}
                        {{else}}
                            $0
                        {{/if}}
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{else}}
            <p style="text-align:center; color:#fff;">No bets yet.</p>
        {{/if}}
    </div>
</body>
</html> 