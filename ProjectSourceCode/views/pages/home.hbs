<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Buff Bets</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #cfb87c; /* CU Boulder Gold */
            color: #000; /* Black Text */
            text-align: center;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 80%;
            margin: 50px auto;
        }

        .card {
            background: #000; /* Black Background */
            color: #cfb87c; /* CU Boulder Gold */
            padding: 20px;
            margin: 15px;
            border-radius: 10px;
            box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.3);
            display: inline-block;
            width: 30%;
        }

        .card h3 {
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Upcoming Games</h2>
        {{#each events}}
            <div class="card">
                <h3>{{date}}</h3>
                <p>{{home_team}} vs {{away_team}}</p>
                
                {{!-- <p>{{odds[0].team}}: {{odds[0].moneyline}}</p>
                <p>{{odds[1].team}}: {{odds[1].moneyline}}</p>                 --}}
                {{#each odds}}
                    <p>{{team}}: {{moneyline}}</p>
                {{/each}}
            </div>
        {{/each}}
    </div>
</body>
</html>
